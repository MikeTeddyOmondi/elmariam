{
  "name": "@elmariam/checkout",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@elmariam/queue": "workspace:*",
    "hono": "^4.0.0",
    "intasend-node": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
